function ppaDisplay(model)

% PPADISPLAY Display parameters of PPA model.
% FORMAT
% DESC Display parameters of PPA model.
% ARG model : The current ppa model.

% PPA

noiseDisplay(model.noise);
kernDisplay(model.kern);